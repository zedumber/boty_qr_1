# 🗺️ Guía Visual de la Arquitectura

## 📊 Diagrama de Flujo - Recepción de Mensaje

```
┌─────────────────────────────────────────────────────────────────┐
│                        WhatsApp Cloud                           │
│                    (Usuario envía mensaje)                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Baileys Socket (sock)                        │
│              sock.ev.on("messages.upsert")                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                  WhatsAppManager.js                             │
│        Agrega mensaje a cola Bull/Redis                        │
│        queueManager.addMessageToQueue()                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Cola Redis (Bull)                           │
│            Procesamiento concurrente (5 max)                    │
│              Con reintentos automáticos                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│               QueueManager.processMessages()                    │
│            Ejecuta procesador para cada job                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              MessageReceiver.processMessage()                   │
│                                                                 │
│  1. Validar mensaje (no vacío, no propio)                     │
│  2. Validar JID (solo usuarios individuales)                  │
│  3. Resolver LID → lidResolver.resolveLid()                   │
│  4. Extraer contenido (texto, audio, imagen, etc.)           │
│  5. Descargar archivos multimedia si aplica                   │
│  6. Enviar a Laravel webhook                                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    lidResolver.resolveLid()                     │
│                                                                 │
│  Estrategia 1: jidNormalizedUser(remoteJidAlt)                │
│  ├─ ✅ Éxito → Retornar número                                 │
│  └─ ❌ Fallo → Intentar Estrategia 2                           │
│                                                                 │
│  Estrategia 2: Extraer de @s.whatsapp.net                     │
│  ├─ ✅ Es @s.whatsapp.net → Retornar número                   │
│  └─ ❌ No es → Intentar Estrategia 3                           │
│                                                                 │
│  Estrategia 3: Leer lid-mapping-*_reverse.json                │
│  ├─ ✅ Archivo existe → Leer y retornar número                │
│  └─ ❌ No existe → Intentar Estrategia 4                       │
│                                                                 │
│  Estrategia 4: Fallback (extraer y loggear)                   │
│  └─ ⚠️ Retornar lo que se pueda + LOG WARNING                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                 Laravel API Webhook                             │
│      POST /api/whatsapp-webhook/{token}                        │
│                                                                 │
│  Datos enviados:                                               │
│  - from: "573001234567" (número resuelto)                     │
│  - text: "Mensaje del usuario"                                │
│  - type: "text|audio|image|video|document"                    │
│  - wamId: "mensaje-id"                                        │
│  - pushName: "Nombre del contacto"                            │
│  - audio: (archivo si aplica)                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📤 Diagrama de Flujo - Envío de Mensaje

```
┌─────────────────────────────────────────────────────────────────┐
│                    Laravel Backend                              │
│            POST http://node:4000/send-message                  │
│                                                                 │
│  Body: {                                                        │
│    session_id: "uuid",                                         │
│    wa_id: "573001234567",                                      │
│    type: "text",                                               │
│    body: "Hola desde Laravel"                                 │
│  }                                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      index_new.js                               │
│             app.post("/send-message", ...)                      │
│                                                                 │
│  1. Validar que session_id existe                              │
│  2. Llamar messageSender.sendMessage()                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              MessageSender.sendMessage()                        │
│                                                                 │
│  1. Obtener socket de whatsappManager.sessions                │
│  2. Construir JID: "573001234567@s.whatsapp.net"              │
│  3. Llamar sendWithRetry() según tipo                         │
│     ├─ text → sendText()                                       │
│     ├─ image → sendImage()                                     │
│     ├─ audio → sendAudio()                                     │
│     ├─ video → sendVideo()                                     │
│     └─ document → sendDocument()                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│           MessageSender.sendWithRetry()                         │
│                                                                 │
│  Intento 1:                                                     │
│  ├─ sock.sendMessage(jid, content)                            │
│  ├─ ✅ Éxito → Retornar resultado                              │
│  └─ ❌ Error → Esperar 2s, Intentar 2                          │
│                                                                 │
│  Intento 2:                                                     │
│  ├─ sock.sendMessage(jid, content)                            │
│  ├─ ✅ Éxito → Retornar resultado                              │
│  └─ ❌ Error → Esperar 4s, Intentar 3                          │
│                                                                 │
│  Intento 3:                                                     │
│  ├─ sock.sendMessage(jid, content)                            │
│  ├─ ✅ Éxito → Retornar resultado                              │
│  └─ ❌ Error → Throw error                                     │
│                                                                 │
│  Timeout: 15 segundos por intento                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Baileys Socket                               │
│                sock.sendMessage()                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    WhatsApp Cloud                               │
│               (Usuario recibe mensaje)                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Diagrama de Flujo - Inicio de Sesión

```
┌─────────────────────────────────────────────────────────────────┐
│                    Laravel Frontend                             │
│              Usuario hace clic en "Conectar"                   │
│         POST http://node:4000/start {"user_id": 123}          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      index_new.js                               │
│                app.post("/start", ...)                          │
│                                                                 │
│  1. Generar sessionId = uuid()                                 │
│  2. Llamar whatsappManager.startSession()                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│          WhatsAppManager.startSession()                         │
│                                                                 │
│  1. Crear directorio: auth/{sessionId}/                        │
│  2. Cargar credenciales: useMultiFileAuthState()               │
│  3. Crear socket: makeWASocket()                               │
│  4. Configurar event listeners:                                │
│     ├─ connection.update                                       │
│     ├─ messages.upsert                                         │
│     └─ creds.update                                            │
│  5. Guardar en whatsappManager.sessions[sessionId]             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Baileys: connection.update                         │
│                     qr: "base64..."                             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│         WhatsAppManager.handleQrCode()                          │
│                                                                 │
│  1. Verificar si sesión activa en Laravel                      │
│  2. De-duplicación: ¿QR cambió?                               │
│  3. Throttle: ¿Pasaron 30s desde último envío?                │
│  4. Si OK:                                                      │
│     ├─ POST /api/qr → Enviar QR a Laravel                     │
│     ├─ POST /api/whatsapp/status → estado_qr="pending"        │
│     └─ Configurar timeout de 60s para expirar QR              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Laravel Backend                              │
│          Guarda QR en base de datos                            │
│     WhatsappAccount: qr, estado_qr="pending"                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Laravel Frontend                             │
│              Muestra QR al usuario                              │
│           Usuario escanea con WhatsApp móvil                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              Baileys: connection.update                         │
│                  connection: "open"                             │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│       WhatsAppManager.handleSessionOpen()                       │
│                                                                 │
│  1. Limpiar timeouts de QR                                     │
│  2. Limpiar cache de QR                                        │
│  3. POST /api/whatsapp/status → estado_qr="active"            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Laravel Backend                              │
│      Actualiza: estado_qr="active"                             │
│      Usuario puede enviar/recibir mensajes                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Arquitectura de Módulos

```
┌────────────────────────────────────────────────────────────────┐
│                          index_new.js                          │
│                     (Orquestador Principal)                    │
│                                                                │
│  - Express Server                                              │
│  - Rutas API                                                   │
│  - Inicialización de módulos                                  │
│  - Shutdown graceful                                           │
└────┬────────────────┬────────────────┬────────────────┬────────┘
     │                │                │                │
     ▼                ▼                ▼                ▼
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│WhatsApp │    │ Message │    │ Message │    │  Queue  │
│ Manager │    │Receiver │    │ Sender  │    │ Manager │
└────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘
     │              │              │              │
     │              ▼              │              │
     │         ┌─────────┐         │              │
     │         │   LID   │         │              │
     │         │Resolver │         │              │
     │         └─────────┘         │              │
     │                             │              │
     ▼                             ▼              ▼
┌─────────────────────────────────────────────────────┐
│                    Utilidades                        │
│                                                      │
│  - logger.js (Logging estructurado)                 │
│  - config.js (Configuración centralizada)           │
└─────────────────────────────────────────────────────┘
     │                             │
     ▼                             ▼
┌──────────┐              ┌──────────────┐
│  Baileys │              │ Bull + Redis │
│  Socket  │              │    Queue     │
└──────────┘              └──────────────┘
```

---

## 🔍 Flujo Detallado: Resolución de LID

```
                    Mensaje entrante
                 "155147774775462@lid"
                          │
                          ▼
            ┌─────────────────────────┐
            │   lidResolver.js        │
            │   resolveLid()          │
            └───────────┬─────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Estrategia 1 │ │ Estrategia 2 │ │ Estrategia 3 │
│              │ │              │ │              │
│jidNormalized │ │   Direct     │ │  Mapping     │
│   User()     │ │  Extract     │ │   File       │
│              │ │              │ │              │
│ remoteJidAlt │ │@s.whatsapp   │ │lid-mapping-  │
│              │ │    .net      │ │*_reverse.json│
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
       │ ✅ Success     │ ✅ Success     │ ✅ Success
       ├────────────────┼────────────────┤
       │                │                │
       └────────────────┴────────────────┘
                        │
                        ▼
              "573001234567" ✅
                        │
                        ▼
              Retornar a MessageReceiver
                        │
                        ▼
              Enviar a Laravel con
            número real resuelto

       ❌ Todas fallan
            │
            ▼
    Estrategia 4: Fallback
            │
            ├─ Extraer lo que se pueda
            ├─ LOG WARNING detallado
            └─ Retornar número parcial
```

---

## 📊 Flujo de Estados de QR

```
        Usuario hace clic "Conectar"
                    │
                    ▼
        ┌───────────────────────┐
        │   estado_qr: null     │
        │   (No hay cuenta)     │
        └───────────┬───────────┘
                    │
                    ▼ startSession()
        ┌───────────────────────┐
        │ estado_qr: "pending"  │
        │   (Generando QR)      │
        └───────────┬───────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
┌──────────────┐      ┌──────────────┐
│Usuario escanea│      │ QR expira    │
│  QR en 60s   │      │  (60s sin    │
│              │      │   escanear)  │
└──────┬───────┘      └──────┬───────┘
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│estado_qr:    │      │estado_qr:    │
│  "active"    │      │ "inactive"   │
│              │      │              │
│✅ Conectado  │      │❌ Expirado   │
└──────┬───────┘      └──────────────┘
       │
       │ Usuario se desconecta
       ▼
┌──────────────┐
│estado_qr:    │
│ "inactive"   │
│              │
│🔌 Desconect. │
└──────────────┘
```

---

## 🎯 Puntos Clave de la Arquitectura

### 1. **Separación de Responsabilidades**

- Cada módulo tiene UNA responsabilidad clara
- No hay código duplicado
- Fácil de mantener y testear

### 2. **Flujo Asíncrono con Colas**

- Mensajes entrantes → Cola Bull/Redis
- Procesamiento concurrente (5 workers)
- Reintentos automáticos
- No se pierden mensajes

### 3. **Resolución Inteligente de LIDs**

- 4 estrategias en cascada
- Logging detallado
- Fallback seguro
- No falla silenciosamente

### 4. **Manejo Robusto de QR**

- De-duplicación (no enviar el mismo QR)
- Throttling (máx 1 cada 30s)
- Expiración automática (60s)
- Limpieza de estado

### 5. **Observabilidad**

- Logs estructurados en JSON
- Métricas de rendimiento
- Health checks
- Circuit breaker

---

## 📝 Orden de Ejecución al Inicio

```
1. startServer()
   │
   ├─► 2. initializeModules()
   │      │
   │      ├─► QueueManager.initialize()
   │      │   └─► Conectar a Redis
   │      │
   │      ├─► new WhatsAppManager()
   │      │
   │      ├─► new MessageReceiver()
   │      │
   │      ├─► new MessageSender()
   │      │
   │      └─► queueManager.processMessages()
   │          └─► Configurar workers
   │
   ├─► 3. whatsappManager.restoreSessions()
   │      │
   │      ├─► GET /api/whatsapp/accounts/active
   │      │
   │      └─► Para cada cuenta activa:
   │          └─► startSession(sessionId, userId)
   │
   └─► 4. app.listen(PORT)
       └─► Servidor HTTP escuchando
```

---

Esta guía visual te ayuda a entender cómo fluyen los datos y cómo interactúan los módulos. 🎯
